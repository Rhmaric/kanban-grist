{
  "name": "kanban-grist",
  "private": true,
  "scripts": {
    "test": "node --test",
    "lint": "eslint .",
    "vendor:update": "node scripts/update-vendor.js",
    "check:vendor": "node scripts/check-vendor.js",
    "bundle": "node scripts/bundle.js",
    "check:dist": "node scripts/check-dist.js",
    "check": "npm run lint && npm test && npm run check:vendor && npm run bundle && npm run check:dist"
  },
  "devDependencies": {
    "@eslint/js": "9.39.5",
    "eslint": "9.39.5",
    "eslint-plugin-no-unsanitized": "4.1.5",
    "globals": "17.12.0"
  }
}
